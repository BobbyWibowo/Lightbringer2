// TODO: Setup file
// NOTE: For now just run the bot.js file normally (yarn start). It will create the config file with default values. Afterwards you'll simply have to edit the file and run the bot again (values that you need to fill into the config file should be rather self-explanatory).
