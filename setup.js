// TODO: Setup script with simple prompt I guess?

const Logger = require('./src/util/Logger')

Logger.warn('Setup script is not yet available.')
Logger.info('Instead, run "node lightbringer2.js" once, to generate a template config.json file.')
Logger.info('Edit the file with your desired configuration, then run "node lightbringer2.js" again to start the bot.')
process.exit(0)
